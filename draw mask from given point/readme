该项目完成以下功能：

给定一个 坐标点集 生成对应的mask图。

如14-16行为一个示例

x_position_list是 点集的x坐标，放在一个List中

y_position_list是 点集的y坐标，放在另一个list中

draw_mask函数：
第一个参数：横坐标List
第二个参数：纵坐标List
第三、四个参数：图像的尺寸（图像的宽和长）
第五个参数：最后生成的mask储存位置

new22.py python用字典 统计有什么种类。字典用法比较经典

find_FirstPIc.py 从一堆图片里拿出 尾号为000 的图片， 放在另一个文件夹中（如果没有该文件夹则创建一个）

find_trainNum (2).py 先将txt中的 files写入list中。 遍历文件夹中文件，如果在list中  
就做下一步：  打开该文件（xml)  做字符串判断  并顺便统计   如遇到是 6_vue_3的类别 就存下他的xml和jpg

delect_txt.py 从大文件中删除小文件
